# Functions in Buff

Buff functions use the `func` keyword followed by the name, parameters, and a colon. The body is indented:

```buff
func greet(name: String):
    print("Hello, " + name)
```

## Your task

Define a `greet` function and call it from `main`.
