// Collections: Vector<T> basics
// Buff's `Vector<T>` lowers to Rust's `Vec<T>`. Literal: `[1, 2, 3]`.
// Operations: `.push(x)` (mutate, needs `let mut`), `.pop()` (returns Option<T>),
// `.len()` (returns Int). Build a stack, push 3 values, pop and print.

func main():
    // TODO: Declare `let mut stack = [1, 2]`, push 3, print len, pop and print.
    print("TODO")
