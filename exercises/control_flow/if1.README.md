# If/Else in Buff

Buff uses indentation for control flow blocks. No braces needed:

```buff
if x > 0:
    print("positive")
else if x == 0:
    print("zero")
else:
    print("negative")
```

## Your task

Implement the `classify` function described in the exercise file.
