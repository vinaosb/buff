// Buff v0.1 example — a simple calculator exercising function calls.
// Run: `buff run examples/calculadora.buff`
func add(a: Int, b: Int) -> Int:
    return a + b

func main():
    print(add(2, 3))
