import rk4 from buff.science

func main():
    // Solve dy/dt = y, y(0) = 1. Exact: y(1) = e ~ 2.71828.
    let result = rk4(fn(t, y) => y, 1.0, 0.0, 1.0, 0.01)
    print("RK4 result:", result)
    print("Expected e:", 2.718281828)
