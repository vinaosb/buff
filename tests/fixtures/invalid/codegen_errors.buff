// INVALID — codegen error: E1301 (unsupported language feature)
// A captured mutation in a parallel closure triggers a race-condition
// rejection in the codegen's race_analysis pre-pass.
func main():
    let mut total = 0
    [1, 2, 3].par_map({ x => total = total + x })
    print(total)
