# Generated by wasm-bindgen (regenerate via the build steps in README.md).
# The pkg/ directory is a build artifact, not source.
pkg/

# Playwright test artifacts.
test-results/
playwright-report/

# Node modules.
node_modules/

# Local evidence / screenshots captured during manual testing (CI captures
# its own under .sisyphus/evidence/).
.scratch/
