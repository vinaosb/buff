{
  "name": "buff-playground-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright tests for the Buff Wasm playground (T114).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "serve": "python -m http.server 8092 --bind 127.0.0.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
