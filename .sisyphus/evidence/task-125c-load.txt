T125c Acceptance Evidence — :load file + call loaded function
================================================================
Command:
  ":help`n:load examples/fibonacci.buff`nfib(10)`n:quit" | buff repl

Output:
  Buff REPL commands:
    :help              Show this help message
    :type <expr>       Print the inferred type of <expr>
    :load <path>       Load a .buff file's declarations into the session
    :quit              Exit the REPL (same as Ctrl-D)

  Anything else is evaluated as a Buff expression or statement.
  Press Ctrl-D or Ctrl-C to exit. Multi-line input: end a block with a blank line.
  :load `examples/fibonacci.buff`: 1 decl(s) loaded, 1 main(s) skipped
  55
  bye.

Result: PASS. Loaded decl persists into session; fib(10) => 55.

NOTE: The plan acceptance text (L1690) reads "fibonacci(10)", but the
example's function is named `fib` (examples/fibonacci.buff defines
`func fib(n: Int) -> Int`). The correct call is `fib(10)`, which yields 55.
The `func main` in the file is correctly skipped on load.
