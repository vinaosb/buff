[package]
name = "buff-lang-error"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
thiserror.workspace = true

[dev-dependencies]
# T36: snapshot tests for stable error-message rendering (public API).
insta.workspace = true
