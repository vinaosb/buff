---
source: crates/buff-lang-codegen-rust/tests/geo_codegen.rs
assertion_line: 501
expression: src
---
fn main() {
    let p: buff_geo::Point = buff_geo::Point::new(1.0f64, 2.0f64);
    let q: buff_geo::Point = buff_geo::Point::new(4.0f64, 6.0f64);
    p.distance_to(q);
    let ring = buff_geo::LineString::from_coords(coords).unwrap_or_default();
    let poly = buff_geo::Polygon::new(ring).unwrap_or_default();
    poly.area;
    poly.clone().contains(p.clone());
    {
        if let Ok(__buff_contents) = std::fs::read_to_string(".env") {
            for __buff_line in __buff_contents.lines() {
                let __buff_line = __buff_line.trim();
                if __buff_line.is_empty() || __buff_line.starts_with('#') {
                    continue;
                }
                if let Some((__buff_key, __buff_val)) = __buff_line.split_once('=') {
                    let __buff_k = __buff_key.trim().to_string();
                    let __buff_v = __buff_val.trim().to_string();
                    if !__buff_k.is_empty() && std::env::var(&__buff_k).is_err() {
                        unsafe {
                            std::env::set_var(&__buff_k, &__buff_v);
                        }
                    }
                }
            }
        }
    }
}
