[package]
name = "buff-lang-runtime"
version = "1.2.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
thiserror.workspace = true
buff-lang-error.workspace = true
# T24: panic hook installer for Buff-span stack traces. Consumes ONLY
# the public `install_panic_hook` function; does NOT add any compute
# logic to this crate. The hook is registered transparently at runtime
# init so user programs see Buff-source-mapped traces automatically.
buff-lang-debug-info.workspace = true
rayon.workspace = true
tokio.workspace = true
wgpu.workspace = true
bytemuck.workspace = true
pollster.workspace = true

[dev-dependencies]
insta.workspace = true
buff-lang-ast.workspace = true
buff-lang-codegen-wgsl.workspace = true
