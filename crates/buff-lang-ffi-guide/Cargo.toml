[package]
name = "buff-lang-ffi-guide"
version = "1.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "Documentation-only crate: FFI safety rules and conventions for Buff extern bindings"

[dependencies]
