[package]
name = "buff-lang-types"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[dependencies]
thiserror.workspace = true
buff-lang-error.workspace = true
buff-lang-ast.workspace = true

[dev-dependencies]
insta.workspace = true
# T28: option_null_safety tests verify None/Some are NOT reserved lexer
# keywords (they resolve as prelude Option variants).
buff-lang-lexer.workspace = true
